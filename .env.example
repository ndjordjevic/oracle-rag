# Copy to .env and fill in your values.

# --- LLM (required for generation) ---
# Provider: openai (default) | anthropic
# ORACLE_RAG_LLM_PROVIDER=openai
# Model name (provider-specific). If unset, uses provider default.
# ORACLE_RAG_LLM_MODEL=gpt-4o-mini

# OpenAI (default LLM)
OPENAI_API_KEY=your_openai_api_key_here

# Anthropic (when ORACLE_RAG_LLM_PROVIDER=anthropic)
# ANTHROPIC_API_KEY=your_anthropic_api_key_here

# --- Embeddings (required for indexing and retrieval) ---
# Provider: openai (default) | cohere
# ORACLE_RAG_EMBEDDING_PROVIDER=openai
# Model name. OpenAI: text-embedding-3-small, text-embedding-3-large.
# Cohere: embed-english-v3.0, embed-english-light-v3.0, embed-multilingual-v3.0
# ORACLE_RAG_EMBEDDING_MODEL=text-embedding-3-small

# Cohere (when ORACLE_RAG_EMBEDDING_PROVIDER=cohere; requires: pip install oracle-rag[cohere])
# COHERE_API_KEY=your_cohere_api_key_here

# --- Optional: LangSmith tracing (see notes/langsmith-setup.md) ---
# LANGCHAIN_API_KEY=your_langsmith_api_key_here
# LANGCHAIN_TRACING_V2=true
# LANGCHAIN_PROJECT=oracle-rag

# Optional: Chroma vector store directory (default: ~/.oracle-rag/chroma_db)
# ORACLE_RAG_PERSIST_DIR=~/.oracle-rag/chroma_db

# Optional: Chunking (defaults: 1000, 200)
# ORACLE_RAG_CHUNK_SIZE=1000
# ORACLE_RAG_CHUNK_OVERLAP=200
